{
  "name": "novatech-seo-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free static editorial site for practical AI and technology.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "dev": "node scripts/server.mjs",
    "check": "pnpm validate && pnpm build && pnpm validate"
  },
  "engines": { "node": ">=22" }
}
