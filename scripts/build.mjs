import { readFile, writeFile, mkdir, rm, cp } from 'node:fs/promises';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const dist = path.join(root, 'dist');
const site = JSON.parse(await readFile(path.join(root, 'config/site.json'), 'utf8'));
const posts = JSON.parse(await readFile(path.join(root, 'content/posts.json'), 'utf8'));
const esc = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const url = pathname => `${site.baseUrl}${pathname}`;
const words = post => post.blocks.map(block => [block.text, ...(block.items || []).flatMap(i => typeof i === 'string' ? [i] : [i.title, i.text, i.question, i.answer]), ...(block.rows || []).flat()].filter(Boolean).join(' ')).join(' ').replace(/<[^>]+>/g, ' ').trim().split(/\s+/).filter(Boolean).length;
const reading = post => Math.max(1, Math.ceil(words(post) / 220));
const fmt = date => new Intl.DateTimeFormat('en-SA', { day:'numeric', month:'long', year:'numeric', timeZone:'UTC' }).format(new Date(`${date}T00:00:00Z`));

function header(active = '') {
  return `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="shell nav-wrap"><a class="brand" href="/" aria-label="Novatech home"><span class="brand-mark">N</span><span>NOVATECH</span></a><button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav"><span class="sr-only">Toggle menu</span><span></span><span></span><span></span></button><nav id="site-nav" aria-label="Primary navigation"><a ${active==='home'?'aria-current="page"':''} href="/">Home</a><a ${active==='blog'?'aria-current="page"':''} href="/blog/">Blog</a><a ${active==='about'?'aria-current="page"':''} href="/about/">About</a></nav></div></header>`;
}
function footer() {
  return `<footer class="site-footer"><div class="shell footer-grid"><div><a class="brand brand-footer" href="/"><span class="brand-mark">N</span><span>NOVATECH</span></a><p>${esc(site.tagline)}</p></div><div><strong>Explore</strong><a href="/blog/">All articles</a><a href="/about/">About</a><a href="/rss.xml">RSS feed</a></div><div><strong>From Saudi Arabia</strong><p>Built for curious, practical people.</p><a href="/about/">Our editorial approach</a></div></div><div class="shell footer-base"><span>© ${new Date().getUTCFullYear()} ${esc(site.name)}.</span><span>Independent, evidence-led technology publishing.</span></div></footer>`;
}
function layout({ title, description, canonical, body, active='', image='/static/social-card.svg', type='website', jsonLd=[] }) {
  const fullTitle = title === site.name ? `${site.name} — ${site.tagline}` : `${title} | ${site.name}`;
  return `<!doctype html><html lang="en-SA"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(fullTitle)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${esc(canonical)}"><link rel="icon" href="/static/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="/static/styles.css"><link rel="alternate" type="application/rss+xml" title="Novatech RSS" href="/rss.xml"><meta property="og:type" content="${type}"><meta property="og:site_name" content="${esc(site.name)}"><meta property="og:title" content="${esc(fullTitle)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${esc(canonical)}"><meta property="og:image" content="${url(image)}"><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${esc(fullTitle)}"><meta name="twitter:description" content="${esc(description)}"><meta name="twitter:image" content="${url(image)}">${jsonLd.map(data => `<script type="application/ld+json">${JSON.stringify(data).replace(/</g,'\\u003c')}</script>`).join('')}</head><body>${header(active)}${body}${footer()}<script src="/static/app.js" defer></script></body></html>`;
}
function renderBlock(block) {
  switch (block.type) {
    case 'paragraph': return `<p>${block.text}</p>`;
    case 'heading': { const level = block.level === 3 ? 3 : 2; const id = block.text.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''); return `<h${level} id="${id}">${esc(block.text)}</h${level}>`; }
    case 'callout': return `<aside class="callout"><strong>${esc(block.title)}</strong><p>${esc(block.text)}</p></aside>`;
    case 'list': return `<ul class="${block.style==='check'?'check-list':''}">${block.items.map(i => `<li>${esc(i)}</li>`).join('')}</ul>`;
    case 'steps': return `<ol class="steps">${block.items.map((i,n) => `<li><span>${n+1}</span><div><strong>${esc(i.title)}</strong><p>${esc(i.text)}</p></div></li>`).join('')}</ol>`;
    case 'table': return `<div class="table-wrap"><table><thead><tr>${block.headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${block.rows.map(row=>`<tr>${row.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
    case 'faq': return `<div class="faq-list">${block.items.map(i=>`<details><summary>${esc(i.question)}</summary><p>${esc(i.answer)}</p></details>`).join('')}</div>`;
    default: throw new Error(`Unknown block type: ${block.type}`);
  }
}
function card(post) { return `<article class="story-card" data-article data-category="${esc(post.category.toLowerCase())}" data-search="${esc(`${post.title} ${post.excerpt} ${post.tags.join(' ')}`.toLowerCase())}"><a class="card-link" href="/blog/${post.slug}/"><span class="eyebrow">${esc(post.category)}</span><h2>${esc(post.title)}</h2><p>${esc(post.excerpt)}</p><span class="card-meta">${fmt(post.published)} · ${reading(post)} min read <b aria-hidden="true">→</b></span></a></article>`; }

await rm(dist, { recursive:true, force:true });
await mkdir(dist, { recursive:true });
await cp(path.join(root, 'static'), path.join(dist, 'static'), { recursive:true });

const featured = posts.find(p => p.featured) || posts[0];
const categories = [...new Set(posts.map(p => p.category))];
const homeBody = `<main id="main"><section class="hero"><div class="shell hero-grid"><div><span class="eyebrow">Technology, made useful</span><h1>Ideas you can<br><em>put to work.</em></h1><p>${esc(site.tagline)} Clear guides for people building and growing in Saudi Arabia.</p><div class="hero-actions"><a class="button primary" href="/blog/">Read the latest</a><a class="text-link" href="/about/">Why Novatech <span>→</span></a></div></div><div class="signal-panel" aria-label="Our editorial signal"><span class="signal-label">NOVATECH / SIGNAL 001</span><div class="signal-lines"><i></i><i></i><i></i><i></i><i></i></div><strong>Useful over noisy.</strong><p>We test ideas against practical constraints: time, cost, security, and real people.</p></div></div></section><section class="section shell"><div class="section-head"><div><span class="eyebrow">Featured story</span><h2>Start here</h2></div><a class="text-link" href="/blog/">View all articles <span>→</span></a></div><article class="featured"><div class="feature-index">01</div><div><span class="pill">${esc(featured.category)}</span><h2><a href="/blog/${featured.slug}/">${esc(featured.title)}</a></h2><p>${esc(featured.excerpt)}</p><div class="article-meta"><span>By ${esc(featured.author)}</span><span>${fmt(featured.published)}</span><span>${reading(featured)} min read</span></div></div></article></section><section class="category-band"><div class="shell"><div class="section-head"><div><span class="eyebrow">What we cover</span><h2>Built for practical curiosity</h2></div></div><div class="category-grid"><div><span>01</span><h3>Practical AI</h3><p>Workflows, agents, and adoption without magical thinking.</p></div><div><span>02</span><h3>Useful tools</h3><p>Software worth learning, with its limits clearly stated.</p></div><div><span>03</span><h3>Emerging tech</h3><p>Signals that matter beyond the launch-day noise.</p></div><div><span>04</span><h3>Tech business</h3><p>Building durable products and companies in the region.</p></div></div></div></section><section class="section shell"><div class="section-head"><div><span class="eyebrow">Latest</span><h2>Recent field notes</h2></div></div><div class="cards">${posts.slice(0,3).map(card).join('')}</div></section><section class="newsletter shell"><div><span class="eyebrow">The useful signal</span><h2>One considered note, when there is something worth saying.</h2></div><div class="newsletter-preview" aria-labelledby="newsletter-status"><span id="newsletter-status" class="status-badge">Coming soon</span><div><input aria-label="Email address preview" type="email" placeholder="you@company.com" disabled><button class="button primary" type="button" disabled>Subscribe</button></div><small>The Novatech newsletter is not accepting subscriptions yet.</small></div></section><section class="about-strip"><div class="shell"><span class="eyebrow">About Novatech</span><p>We publish grounded explanations for founders, operators, and curious professionals navigating technology in Saudi Arabia.</p><a class="text-link" href="/about/">Our editorial approach <span>→</span></a></div></section></main>`;
await writeFile(path.join(dist,'index.html'), layout({title:site.name, description:site.description, canonical:url('/'), body:homeBody, active:'home'}));

const blogBody = `<main id="main"><section class="page-hero shell"><span class="eyebrow">Novatech journal</span><h1>Practical technology,<br>carefully explained.</h1><p>Guides, operating notes, and honest evaluations for ambitious teams.</p></section><section class="shell listing"><div class="filters"><label for="article-search">Search articles</label><input id="article-search" type="search" placeholder="Search by title, topic, or keyword"><div class="filter-buttons" aria-label="Filter by category"><button class="active" data-filter="all">All</button>${categories.map(c=>`<button data-filter="${esc(c.toLowerCase())}">${esc(c)}</button>`).join('')}</div></div><p id="search-status" class="search-status" aria-live="polite">${posts.length} article${posts.length===1?'':'s'}</p><div class="cards cards-list">${posts.map(card).join('')}</div><div id="no-results" class="no-results" hidden><h2>No matching articles</h2><p>Try a broader search or clear the category filter.</p></div></section></main>`;
await mkdir(path.join(dist,'blog'),{recursive:true});
await writeFile(path.join(dist,'blog','index.html'), layout({title:'Blog', description:`Browse ${site.name} guides on practical AI, emerging technology, useful tools, and technology business.`, canonical:url('/blog/'), body:blogBody, active:'blog'}));

for (const post of posts) {
  const postUrl = url(`/blog/${post.slug}/`);
  const faq = post.blocks.find(b=>b.type==='faq');
  const articleLd = {'@context':'https://schema.org','@type':'BlogPosting',headline:post.title,description:post.description,datePublished:post.published,dateModified:post.updated,author:{'@type':'Person',name:post.author},publisher:{'@type':'Organization',name:site.name,logo:{'@type':'ImageObject',url:url('/static/favicon.svg')}},mainEntityOfPage:postUrl,image:url('/static/social-card.svg'),inLanguage:'en-SA',keywords:post.tags.join(', '), ...(faq?{mainEntity:faq.items.map(i=>({'@type':'Question',name:i.question,acceptedAnswer:{'@type':'Answer',text:i.answer}}))}:{})};
  const breadcrumbLd = {'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:url('/')},{'@type':'ListItem',position:2,name:'Blog',item:url('/blog/')},{'@type':'ListItem',position:3,name:post.title,item:postUrl}]};
  const related = posts.filter(p=>p.slug!==post.slug).slice(0,2);
  const content = post.blocks.map(renderBlock).join('');
  const articleBody = `<main id="main"><article><header class="article-header"><div class="narrow"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><a href="/blog/">Blog</a><span>/</span><span>${esc(post.category)}</span></nav><span class="eyebrow">${esc(post.category)}</span><h1>${esc(post.title)}</h1><p class="dek">${esc(post.description)}</p><div class="article-meta"><span>By <strong>${esc(post.author)}</strong></span><time datetime="${post.published}">${fmt(post.published)}</time><span>${reading(post)} min read</span></div></div></header><div class="article-grid shell"><aside class="article-rail"><span>In this guide</span><nav>${post.blocks.filter(b=>b.type==='heading'&&b.level===2).map(b=>`<a href="#${b.text.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}">${esc(b.text)}</a>`).join('')}</nav></aside><div class="prose">${content}<section class="sources"><h2 id="sources">Sources and further reading</h2><ol>${post.sources.map(s=>`<li><a href="${esc(s.url)}" rel="noopener noreferrer">${esc(s.title)}</a> <span>— ${esc(s.publisher)}</span></li>`).join('')}</ol></section><section class="share"><strong>Share this guide</strong><div><a href="https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(postUrl)}" rel="noopener noreferrer">LinkedIn</a><a href="https://twitter.com/intent/tweet?url=${encodeURIComponent(postUrl)}&text=${encodeURIComponent(post.title)}" rel="noopener noreferrer">X / Twitter</a><a href="mailto:?subject=${encodeURIComponent(post.title)}&body=${encodeURIComponent(postUrl)}">Email</a></div></section></div></div><section class="related shell"><span class="eyebrow">Keep reading</span><h2>${related.length ? 'Related articles' : 'More from Novatech'}</h2>${related.length?`<div class="cards">${related.map(card).join('')}</div>`:`<p>New practical guides are on the way. Browse the journal or follow the RSS feed.</p><a class="button primary" href="/blog/">Browse the journal</a>`}</section></article></main>`;
  const dir = path.join(dist,'blog',post.slug); await mkdir(dir,{recursive:true});
  await writeFile(path.join(dir,'index.html'), layout({title:post.title,description:post.description,canonical:postUrl,body:articleBody,active:'blog',type:'article',jsonLd:[articleLd,breadcrumbLd]}));
}

const aboutBody = `<main id="main"><section class="page-hero shell"><span class="eyebrow">About Novatech</span><h1>Clarity for people<br>building what is next.</h1><p>Novatech is an independent, English-first publication for Saudi Arabia's builders, operators, and technology-curious professionals.</p></section><section class="narrow prose about-copy"><h2>Our purpose</h2><p>Technology coverage often sits at two unhelpful extremes: breathless prediction or dense specialist detail. Novatech takes a third path. We explain what a technology does, where it is useful, what can go wrong, and how a thoughtful team can try it responsibly.</p><h2>Our editorial standard</h2><p>We prefer primary sources, conservative claims, visible limitations, and useful next steps. We do not invent certainty, statistics, customers, or results. When an article touches legal, medical, financial, or regulatory questions, it is a starting point for qualified advice—not a substitute for it.</p><h2>Who writes Novatech</h2><p>Novatech is written and edited by ${esc(site.author)} with a focus on practical AI, emerging technology, useful tools, and the craft of building technology businesses in Saudi Arabia.</p><h2>Stay connected</h2><p>A public contact channel and the Novatech newsletter are coming soon. Until then, every published guide remains available through the journal and RSS feed.</p></section></main>`;
await mkdir(path.join(dist,'about'),{recursive:true});
await writeFile(path.join(dist,'about','index.html'), layout({title:'About',description:`About ${site.name}, an independent practical technology publication for an English-first Saudi audience.`,canonical:url('/about/'),body:aboutBody,active:'about'}));

const notFound = `<main id="main" class="not-found shell"><span class="error-code">404 / SIGNAL LOST</span><h1>This page is off the grid.</h1><p>The address may have changed, or the page may never have existed.</p><a class="button primary" href="/">Return home</a></main>`;
await writeFile(path.join(dist,'404.html'), layout({title:'Page not found',description:'The requested Novatech page could not be found.',canonical:url('/404.html'),body:notFound}));

const entries = posts.map(p=>({loc:`/blog/${p.slug}/`,lastmod:p.updated}));
const sitemap = `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[{loc:'/',lastmod:posts[0]?.updated},{loc:'/blog/',lastmod:posts[0]?.updated},{loc:'/about/',lastmod:posts[0]?.updated},...entries].map(e=>`<url><loc>${url(e.loc)}</loc><lastmod>${e.lastmod}</lastmod></url>`).join('')}</urlset>`;
await writeFile(path.join(dist,'sitemap.xml'),sitemap);
await writeFile(path.join(dist,'robots.txt'),`User-agent: *\nAllow: /\n\nSitemap: ${url('/sitemap.xml')}\n`);
const rss = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${esc(site.name)}</title><link>${site.baseUrl}</link><description>${esc(site.description)}</description><language>${site.language}</language>${posts.map(p=>`<item><title>${esc(p.title)}</title><link>${url(`/blog/${p.slug}/`)}</link><guid>${url(`/blog/${p.slug}/`)}</guid><pubDate>${new Date(`${p.published}T00:00:00Z`).toUTCString()}</pubDate><description>${esc(p.description)}</description></item>`).join('')}</channel></rss>`;
await writeFile(path.join(dist,'rss.xml'),rss);
await writeFile(path.join(dist,'feed.json'),JSON.stringify({version:'https://jsonfeed.org/version/1.1',title:site.name,home_page_url:site.baseUrl,feed_url:url('/feed.json'),description:site.description,authors:[{name:site.author}],items:posts.map(p=>({id:url(`/blog/${p.slug}/`),url:url(`/blog/${p.slug}/`),title:p.title,summary:p.description,date_published:`${p.published}T00:00:00Z`,date_modified:`${p.updated}T00:00:00Z`,tags:p.tags}))},null,2));
console.log(`Built ${posts.length} article and ${4 + posts.length} HTML pages in dist/`);
